from setuptools import setup, find_packages

setup(
    name="ft_package",  # Replace with your package name
    version="0.1.0",  # Replace with your initial version
    description="My first package",
    author="Alp a. Yalman",
    author_email="your_email@example.com",
    packages=find_packages(),  # Automatically finds packages
    install_requires=[],  # Optional: List dependencies if needed
)